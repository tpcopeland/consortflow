% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf_exclude.R
\name{cf_exclude}
\alias{cf_exclude}
\title{Exclude observations from the flowchart}
\usage{
cf_exclude(obj, condition, label, remaining = NULL)
}
\arguments{
\item{obj}{A \code{consortflow} object.}

\item{condition}{An unquoted expression evaluated in the context of the
current data (e.g., \code{age < 18}). Rows where this is \code{TRUE} are excluded.
Rows where the result is \code{NA} are kept (not excluded).}

\item{label}{Character string describing this exclusion
(e.g., \code{"Age < 18 years"}).}

\item{remaining}{Optional character label for the remaining-population box
after this step (e.g., \code{"Adult cohort"}). If \code{NULL}, no label is shown
on the remaining box.}
}
\value{
The updated \code{consortflow} object (returned invisibly).
}
\description{
Apply an exclusion criterion. Rows matching the condition are dropped
from the working data and recorded as an exclusion step in the diagram.
}
\examples{
d <- data.frame(id = 1:100, age = sample(15:80, 100, replace = TRUE))
flow <- cf_init(d, label = "Source population")
flow <- cf_exclude(flow, age < 18, label = "Age < 18 years")
print(flow)

}
