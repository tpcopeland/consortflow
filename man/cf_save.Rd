% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf_save.R
\name{cf_save}
\alias{cf_save}
\title{Save the exclusion flowchart to a file}
\usage{
cf_save(
  obj,
  output,
  final = "Final Cohort",
  shading = FALSE,
  dpi = 150L,
  font_family = "sans"
)
}
\arguments{
\item{obj}{A \code{consortflow} object with at least one exclusion step.}

\item{output}{Character path for the output file. The file extension
determines the format: \code{.png}, \code{.pdf}, or \code{.svg}.}

\item{final}{Character label for the final cohort box
(default: \code{"Final Cohort"}).}

\item{shading}{Logical; if \code{TRUE}, main-flow boxes are light blue and
exclusion boxes are light red. Default is \code{FALSE} (white boxes).}

\item{dpi}{Integer resolution for PNG output (default: 150; use 300 for
publication quality). Ignored for PDF/SVG.}

\item{font_family}{Character font family (default: \code{"sans"}).}
}
\value{
The \code{consortflow} object (returned invisibly), with \verb{$output}
set to the file path written.
}
\description{
Render the exclusion flowchart and save it as PNG, PDF, or SVG.
}
\examples{
d <- data.frame(
  id = 1:1000,
  age = sample(15:80, 1000, replace = TRUE),
  fu = sample(1:365, 1000, replace = TRUE)
)
flow <- cf_init(d, label = "Source population")
flow <- cf_exclude(flow, age < 18, label = "Age < 18 years")
flow <- cf_exclude(flow, fu < 30, label = "Follow-up < 30 days")
\donttest{
cf_save(flow, output = tempfile(fileext = ".png"),
        final = "Study population", dpi = 150)
}

}
